/* 
   Arquivo: Aula03.C
   Arquivo único.
   Aula 09/08/2024
   Autor: João Paulo Traguetta Rufino

*/

#include <stdio.h>>

int main() { 
 int n,maior,menor;

  printf("Digite um número inteiro: ");
    scanf("%d", &n);
      maior = n;
      menor = n;
  printf("Digite mais um número inteiro: ");
  scanf("%d", &n);
        if(n>maior){
      maior = valor;
        } 
        if(valor<menor){
      menor = valor;
        }
  printf("Digite mais um número inteiro: ");
  scanf("%d", &n);
        if(n>maior){
      maior = valor;
        } 
        if(valor<menor){
      menor = valor;
        }
  printf("Digite mais um número inteiro: ");
  scanf("%d", &n);
        if(n>maior){
      maior = valor;
        } 
        if(valor<menor){
      menor = valor;
        }
  printf("Digite mais um número inteiro: ");
  scanf("%d", &n);
        if(n>maior){
      maior = valor;
        } 
        if(valor<menor){
      menor = valor;
        }
  printf("O seu maior número é: %d e seu menor número é: %d", maior, menor);
  
  return 0;
}