#include <string.h>
#include <stdio.h>

int main (){
   //strcmp faz uma comparação por odem alfabética
   //strncpy copia um numero de caracteres específicos "strncpy(destino, origem, quantidade de caracteres)"
   //strncat concatena n caracteres de uma string a outra
   //strncmp compara até n caracteres de duas strings "strncmp(duas palavras separadas por vírgula, numero de caracteres)"

   //strchr() localiza a primeira ocorrência de um caractere em uma 
   //strstr() localiza a primeira ocorrência de uma substring em uma string  
   //strtok divide uma string em tokens como base em delimitadores


    return 0;
}